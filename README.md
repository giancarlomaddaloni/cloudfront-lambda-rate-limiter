# cloudfront-lambda-rate-limiter
CloudFront + Lambda@Edge rate-limiting system using client ID tokens and DynamoDB. Enforces per-client throttling at the edge with alerting and AWS CDK deployment support.
